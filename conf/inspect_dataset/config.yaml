defaults:
  - dataset: depo
  - _self_

show: 3
seed: 42
max_tokens: -1
text_chars: 500
tokenizer_name_or_path: null
colorize_loss_mask: true

hydra:
  run:
    dir: outputs/inspect_dataset/${now:%Y-%m-%d_%H-%M-%S}
